language: cpp

compiler: gcc

sudo: require

solution: Trace.sln

branches:
  only:
    - build

install: sudo apt-get install libpcap-dev

before_script:
  - cd build
  - find / -type d -name "libpcap"
  - cmake ../cmake/

notifications:
  email: false
